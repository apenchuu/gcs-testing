version: "2"
authtoken: 36Dr3L8DaXlQF6PmiKLUT5CxAMw_6YskctdwSQShy4cKhV7pY

tunnels:
  # --- FRONTEND (Tetap pakai domain cantik) ---
  frontend:
    addr: 3000
    proto: http
    domain: techily-unregainable-manual.ngrok-free.dev

  # --- BACKEND (Hapus domain, biarkan acak) ---
  backend:
    addr: 8000
    proto: http
    # HAPUS baris 'domain' di sini! 
    # Biarkan kosong agar Ngrok memberi URL random.